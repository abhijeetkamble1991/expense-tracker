apiVersion: v1
kind: Secret
metadata:
  name: {{ include "expense-tracker-backend.secretName" . }}
  labels:
    {{- include "expense-tracker-backend.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
